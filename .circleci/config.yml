# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Orbs are reusable packages of CircleCI configuration that you may share across projects, enabling you to create encapsulated, parameterized commands, jobs, and executors that can be used across multiple projects.
# See: https://circleci.com/docs/2.0/orb-intro/
orbs:
  ruby: circleci/ruby@1.2.0

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  ots-deploy:
    #circleci_ip_ranges: true
    docker:
      - image: 'cimg/ruby:2.4.6'
    executor: ruby/default
    steps:
      - add_ssh_keys:
          fingerprints:
            - "65:1f:4b:bb:c0:08:10:11:e7:ac:3a:8e:c5:48:b1:c5"
      - checkout
      - run:
          name: OTS Deploy
          command: |
            EC2_USERNAME=gust4vo-mlo
            EC2_PUBLIC_DNS=34.71.214.222
            chmod 400 /home/circleci/.ssh/id_rsa_651f4bbbc0081011e7ac3a8ec548b1c5
            ssh -o StrictHostKeyChecking=no -i "/home/circleci/.ssh/id_rsa_651f4bbbc0081011e7ac3a8ec548b1c5" -t -t $EC2_USERNAME@$EC2_PUBLIC_DNS \
            "cd /home/admin/onetimesecret && \
            git pull origin main && \
            docker build -t ots . && \
            docker run -t -p 3000:3000 -d ots\
             -e ONETIMESECRET_REDIS_URL='redis://127.0.0.1:6379/0'";"exit"

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  ots-deploy: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - ots-deploy:
          filters:
            branches:
              only:
                - develop